export const Paragraph = ({ counter, name }) => {
  // console.log(props);

  // const { counter, name } = props;
  console.log("2. rendering .......");
  return (
    <div>
      {counter} - {name}
    </div>
  );
};
